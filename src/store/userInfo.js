import Vue from 'vue'
import vuex from 'vuex'
Vue.use(vuex);

export default new vuex.Store({
    state: null,
    mutations: {
        // getUserInfo(state) {
        // //   state.userInfo = userInfo;
        // console.log('asfasdffssfd')
        // }
      }
})